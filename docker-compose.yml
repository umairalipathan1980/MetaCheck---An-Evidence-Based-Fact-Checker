services:
  backend:
    build:
      context: ./backend
    container_name: metacheck-backend
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: http://localhost:8000
    container_name: metacheck-frontend
    depends_on:
      - backend
    ports:
      - "5173:80"
    restart: unless-stopped
